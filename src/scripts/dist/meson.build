protocols_dir = get_option('prefix') / get_option('datadir') / meson.project_name() / 'protocols'

install_data(
  files(
    'gamespy.js',
    'quake3.js',
    'source.js',
  ),
  install_dir: protocols_dir,
)
